[project]
    name = "fetch-cache-web"
    version = "0.1.0"
    description = "Fetch web content into a named local cache"
    requires-python = ">=3.12"
    dependencies = ["markdownify>=1.2.3"]

[tool.uv]
    package = false
